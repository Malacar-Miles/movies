export type movie = {
    title: string,
    id: string,
    image: string,
    originalTitle: string,
    country: string[],
    language: string[],
    imdbRating: number,
    views: number,
    year: number,
    genre: string[],
    subs: string[],
    director: string[],
    actors: string[],
    description: string
};